<table>
    <tr>
        <td>Nom du bien :
        </td>
        <td>
            <a href="{{ url('property.show',{id: contact.property.id, slug: contact.property.slug}) }}">
                {{contact.property.title}}</a>
        </td>
    </tr>
    <tr>
        <td>Nom</td>
        <td>
            {{contact.lastname}}</td>
        <td>Prénom</td>
        <td>
            {{contact.firstname}}</td>
        <td>Téléphone</td>
        <td>
            {{contact.phone}}</td>
        <td>Email</td>
        <td>
            {{contact.email}}</td>
        <td>Message</td>
        <td>
            {{contact.message|nl2br}}</td>
    </tr>
</table>